apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pv-claim
spec:
  accessModes:
    - ReadWriteOnce   # Dysk może być podpięty tylko do jednego noda na raz
  resources:
    requests:
      storage: 1Gi    # Wielkość dysku