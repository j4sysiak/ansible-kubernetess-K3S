---
- name: Bootstrap K3s container with Python
  hosts: k3s_master
  gather_facts: false
  tasks:
    - name: Update apt cache and install python3
      ansible.builtin.raw: apt-get update && apt-get install -y python3